/* ============================================================
   НАСТРОЙКИ · единственный файл, который нужно править
   ============================================================ */

window.BRIEF_CONFIG = {

  // Адрес Cloudflare Worker — тот же, что у основного брифа.
  // Заявки приходят в один чат, но с разными заголовками.
  ENDPOINT: 'https://soft-cloud-dff2.derzajelianora.workers.dev',

  // Заголовок в сообщении Telegram — по нему видно, чей бриф пришёл
  PROJECT_NAME: 'Новый бриф от Софи'
};
